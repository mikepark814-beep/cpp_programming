```markdown
# [Tetris Project] Day 1 학습 보고서

| 학번 | 이름 | 제출일 |
|------|------|--------|
|202255548|박세준|2026-05-04|

---

## Q1. `board`와 `current_block`을 분리하는 이유를 한 문장으로 설명하시오.

> `board`는 이미 착지한 칸의 영구 상태만 기억하고 `current_block`은 낙하 중인 블록의 임시 모양을 별도로 관리함으로써, 블록이 이동하거나 회전할 때 `board`를 수정하지 않아도 되어 상태 복원이 필요 없기 때문이다.

## Q2. `current_block = {{1, 1, 1, 1}}`과 `{{1},{1},{1},{1}}`의 차이를 행/열로 설명하시오.

> `{{1, 1, 1, 1}}`은 바깥 벡터 원소가 1개(1행), 안쪽 벡터 원소가 4개(4열)인 1행 4열의 가로 막대이고, `{{1},{1},{1},{1}}`은 바깥 벡터 원소가 4개(4행), 안쪽 벡터 원소가 각각 1개(1열)인 4행 1열의 세로 막대이다. I 블록은 가로로 누운 막대이므로 `{{1, 1, 1, 1}}`이 올바른 초기화이다.

---

## 구현 중 막혔던 부분

| 증상 | 원인 | 해결 |
| `state` 케이스에서 `shape: 4 rows, 1 cols` 출력되며 실패 | `current_block`을 `{{1},{1},{1},{1}}`로 초기화해서 4행 1열 세로 막대가 됨. 수학에서 (x, y) 순서에 익숙해서 바깥 인덱스를 열(x)로 착각했음 | `std::vector<std::vector<int>>`에서 바깥이 행(y), 안쪽이 열(x)임을 확인하고 `{{1, 1, 1, 1}}`로 수정 |
| `board_to_string`에서 블록이 엉뚱한 위치에 그려짐 | 루프 변수를 `for(int x ...) for(int y ...)`로 작성해서 행·열이 뒤바뀜 | `current_block[y][x]`에서 바깥이 y(행), 안쪽이 x(열)임을 다시 확인하고 루프 순서를 `for(int y ...) for(int x ...)`로 교정 |
| `board_to_string` 호출 후 렌더링 결과가 `state.board`에 남아 `nonmutating` 케이스 실패 | 처음에 `rendered`를 따로 만들지 않고 `state.board`를 직접 수정했음 | `auto rendered = state.board;`로 깊은 복사본을 만들어 복사본에만 `'#'`을 찍도록 수정 |
```